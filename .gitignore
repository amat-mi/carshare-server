# Specifies intentionally untracked files to ignore when using Git
# http://git-scm.com/docs/gitignore

*.pyc
db.*
media/
static/

# PAOLO - Add secrets Python package files (but not ".template" files)
**/secrets/*.*
!**/secrets/*.template
